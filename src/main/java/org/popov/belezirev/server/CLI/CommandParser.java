package org.popov.belezirev.server.CLI;

public class CommandParser {

	/*
	 * private List<String> extractArgumentsFromString(String argumentsString) {
	 * List<String> extractedArguments = new
	 * ArrayList<>(Arrays.asList(argumentsString.trim().split(" +")));
	 * 
	 * return extractedArguments; }
	 * 
	 */
}
